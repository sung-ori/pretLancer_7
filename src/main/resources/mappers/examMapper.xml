<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.team.pretLancer_7.dao.ExamDAO">
	
	<select id="selectOneExam" resultType="Exam">
	select * from pret_examvalue
	</select>
</mapper>
